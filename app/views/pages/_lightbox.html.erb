<% images.each_with_index do |image, index| %>
<%= link_to image_tag("#{image}.thumbnail.jpg"), asset_path("#{image}.jpg"), :rel => "shadowbox[#{gallery}]", :title => (local_assigns[:titles] && local_assigns[:titles][index] || local_assigns[:title]) %>
<% end %>